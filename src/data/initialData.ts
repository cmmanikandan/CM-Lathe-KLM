import { Product, Order } from '../types';

export const INITIAL_PRODUCTS: Product[] = [];

// No demo orders — all orders come from Supabase
export const INITIAL_ORDERS: Order[] = [];

// No demo stories — all stories come from Supabase (status_stories table)
export const INITIAL_STORIES: never[] = [];
